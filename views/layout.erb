<!doctype html>
<html lang="en" class="no-js">
<head>
	<meta charset="utf-8">
	<title>Melee - Brainstorming done right</title>
	
	<link href='http://fonts.googleapis.com/css?family=Droid+Sans+Mono' rel='stylesheet' type='text/css'>
	<link rel="stylesheet" href="/stylesheets/style.css" type="text/css" media="screen" charset="utf-8">

	<%= erb :'partials/start' %>
	<%= erb :'partials/idea' %>
	<%= erb :'partials/idea_list' %>
	<%= erb :'partials/ideate' %>
	<%= erb :'partials/cluster' %>
	<%= erb :'partials/cluster_list' %>
	<%= erb :'partials/clusterate' %>
	<%= erb :'partials/prioritize' %>
	<%= erb :'partials/export' %>
	<%= erb :'partials/clustered_result_cluster' %>
	<%= erb :'partials/clustered_result_list' %>
	<%= erb :'partials/user' %>
	<%= erb :'partials/message' %>

</head>

<body>
	<script type="text/javascript">
 	 var _gaq = _gaq || [];
 	 _gaq.push(['_setAccount', 'UA-38709118-1']);
 	 _gaq.push(['_trackPageview']);

 	 (function() {
 	   var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
 	   ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
 	   var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
 	 })();
	</script>
	<nav>
		<span id="menu_ideate">
			Ideate
			<div class="arrow"><!-- --></div>
		</span>
		<span id="menu_cluster">
			Cluster
			<div class="arrow"><!-- --></div>
		</span>
		<span id="menu_prioritize">
			Prioritize
			<div class="arrow"><!-- --></div>
		</span>
		<span id="menu_export">
			Export
			<div class="arrow"><!-- --></div>
		</span>
		<span id="menu_collaborate">Team chat<span id="notification_badge">0</span></span>
	</nav>
	<div id="chat">
		<div class="modal"></div>
		<div id="change_nickname">
			<label>Choose a nickname:</label><br/>
			<input type="text" placeholder="Anonymous" />
		</div>
		<div id="users"></div>
		<div id="messages"></div>
		<div id="chat_input">
			<div class="arrow"></div>
			<textarea id="textarea_chat" placeholder="type a message"></textarea>
		</div>
	</div>
	<div id="melee"></div>
	<script src="http://<%= SOCKET_IO_HOST %>/socket.io/socket.io.js"></script>
	<%= javascript_bundle_include_tag(:all) %>
	<script>
		$(function() {
			window.melee = new Melee(<%= yield %>);
			Backbone.history.start({pushState : true});
		});
	</script>
</body>
</html>
