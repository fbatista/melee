{
	unsortedIdeas : new IdeaList(<%= @unsorted %>, {url:'/<%= @sessionid %>/unsorted'}), 
	session : new Session({id : '<%= @sessionid %>'}),
	clusters : new ClusterList(<%= @clusters %>, {url:'/<%= @sessionid %>/clusters'}),
	socket_url : 'http://<%=SOCKET_IO_HOST%>'
}